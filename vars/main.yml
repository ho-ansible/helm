---
helm_mirror: "https://baltocdn.com/helm"
helm_apt_key: "{{ helm_mirror }}/signing.asc"
helm_apt_keyring: /etc/apt/trusted.gpg.d/helm.asc
helm_apt_repo: "deb [signed-by={{ helm_apt_keyring }}] {{ helm_mirror }}/stable/debian/ all main"
helm_bash_completion: /etc/bash_completion.d/helm
